a = 1;
b = 3.3;
a = x;

r = 8;
circ = 2 * r * PI;
area = pow(r,2) * PI;
sqrt(area / PI);

if(area > 20) {
	20;
} 

areaFt = SqrMeterToSqrFeet(area);

rExp = FeetToMeter(sqrt(areaFt / PI));

if(rExp - r < 0.01 AND rExp - r > -0.01) {
	1;
} else {
	0;
}

if(1!=1) {
	1;
} else {
	0;
}